--------------------------------
Box2D (v2.2.0) for Marmalade SDK

 - Box2D is (C) Erin Catto - http://www.box2d.org
 - This archive was created by forum member 'blitter'


----------------------------------------------------
box2d.mkb - use to create your libraries (.lib / .a)

(use only one .mkf *)
box2d.mkf - sub project
box2d_library.mkf - static library version of the sub project (most people will use this)

*DO NOT USE A "STRICT" BUILD


-------------------------------------------
svn: http://box2d.googlecode.com/svn/trunk/


Changes to svn:

b2ContactSolver.cpp
Line 699, inserted:
 psm.normal = psm.point = b2Vec2(0,0);
 psm.separation = 0;

Line 793 - after inserting above:
 psm.normal = psm.point = b2Vec2(0,0);
 psm.separation = 0;


b2TimeOfImpact.cpp
Line 328, inserted:
 fcn.m_localPoint = b2Vec2(0,0);
